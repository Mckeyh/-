<!--pages/home/home.wxml-->
<view class="container">
  <view>
      <text>欢迎使用智慧农机助手</text>
  </view>
  <view>
      <view wx:for="{{features}}" wx:key="title" bindtap="navigateTo" data-url="{{item.url}}">
          <text>{{item.icon}}</text>
          <text>{{item.title}}</text>
          <text>{{item.desc}}</text>
      </view>
  </view>
</view>